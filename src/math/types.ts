/**
 * Calcualte the nth number in the fibonacci sequence.
 * https://en.wikipedia.org/wiki/Fibonacci_number
 */
export type FibonacciFunction = (nth: number) => number;
/**
 * Calcualte the nth number in the pell sequence.
 * https://en.wikipedia.org/wiki/Pell_number
 */
export type PellFunction = (nth: number) => number;
